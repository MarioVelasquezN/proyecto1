// Logic centralized in StockService (src/stock/stock.service.ts).
// This alias preserves backward compatibility for any existing import.
export { StockService as InventoryService } from '../stock/stock.service';
